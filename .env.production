# Life Line — production build config.
# These are PUBLIC keys (shipped to the browser by design). Safe to commit.
# Authorization is enforced by Supabase Row Level Security + SECURITY DEFINER RPCs,
# NOT by keeping these values secret. Never add the service-role/secret key here.
NEXT_PUBLIC_SUPABASE_URL=https://crwapkwvqappomsliass.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=sb_publishable_kLR6ZtubJ588z6f68_fX6Q_jus5fxNt
